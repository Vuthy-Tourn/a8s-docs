import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "deploy-postgresql-cluster": "PostgreSQL Cluster",
  "deploy-mysql-cluster": "MySQL Cluster",
  "deploy-cassandra-cluster": "Cassandra Cluster",
  "deploy-mongodb-cluster": "MongoDB Cluster",
};

export default meta;
