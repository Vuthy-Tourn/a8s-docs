import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "deploy-spring-boot-maven-service": "Spring Boot Microservice with Maven",
  "deploy-spring-boot-gradle-service": "Spring Boot Microservice with Gradle",
};

export default meta;
