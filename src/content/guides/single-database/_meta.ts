import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "deploy-postgresql-single": "Single PostgreSQL Database",
  "deploy-mysql-single": "Single MySQL Database",
  "deploy-sqlserver-single": "Single SQL Server Database",
  "deploy-cassandra-single": "Single Cassandra Cluster Node",
  "deploy-oracle-single": "Single Oracle Database",
  "deploy-mongodb-single": "Single MongoDB Database",
  "deploy-redis-single": "Single Redis Instance",
};

export default meta;
